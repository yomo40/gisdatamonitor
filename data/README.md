# Data

这里存放 `GISDataMonitor` 正式产品的数据底座。

当前首版范围固定为 `江西省`。

DEM 选择规则：

- 只使用可自动化拉取的官方 DEM 数据源
- 显式比较候选源分辨率元数据
- 始终选择当前可用的最高分辨率

目录约定：

- `raw/`：原始下载数据
- `processed/`：裁剪、融合后的正式输入数据
- `manifests/`：下载与处理清单

首版目标：

- 下载江西省边界
- 下载江西覆盖范围内的最高可自动化分辨率 DEM
- 下载并裁剪江西省内全部 Baker 能源设施数据
